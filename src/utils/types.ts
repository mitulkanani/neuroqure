export type ButtonProps = {
  label: string;
  style?: string;
};

export type HeadingProps = {
  label: string;
  style?: string;
};
export type DescriptionProps = {
  description: string;
  style?: string;
};
